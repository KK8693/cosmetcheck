# Cloudflare Pages redirects
# Order matters: more specific rules first

# HTTP -> HTTPS (301 permanent redirect)
# Cloudflare Pages does not support protocol-based redirects in _redirects.
# Use Cloudflare Dashboard > SSL/TLS > Edge Certificates > Always Use HTTPS instead.
# The middleware also enforces HTTPS via x-forwarded-proto detection.

# SPA fallback for Next.js dynamic routes
/* /index.html 200
